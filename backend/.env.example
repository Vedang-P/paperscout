# Backend runtime
PORT=5000
BODY_LIMIT=256kb

# Optional comma-separated origin allowlist for CORS
# Example: CORS_ORIGIN=https://paperscout-seven.vercel.app,http://localhost:5173
CORS_ORIGIN=

# Optional Vercel KV for persistent notes storage
KV_REST_API_URL=
KV_REST_API_TOKEN=
